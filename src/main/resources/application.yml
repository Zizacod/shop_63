#spring.application.name=shop_63

spring:
  application:
    name: Shop_63

  datasource:
    url: jdbc:postgresql://localhost:5433/g_63_shop
    username: my_user
    password: pos1234
    driver-class-name: org.postgresql.Driver

  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml

  jpa:
    properties:
      hibernate:
        auto_quote_keyword: true

server:
  servlet:
    context-path: /api

